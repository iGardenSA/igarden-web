# SITE-ARCHITECTURE.md — igarden.sa

> **مرجع Information Architecture.** يُقرأ مع `PROJECT-STATE.md` ولا يحلّ محلّه.
> **الأساس:** `main = 87cb99b` (#56 مدموج · **Wave 2D ‏LIVE** · `Production id=6069993338`) · **آخر تحديث:** 2026-08-24 · **Site Narrative Rebuild قيد المراجعة**
> **وسم الثقة:** ✓ مُتحقَّق بأمر أو نشر · ◐ مؤجَّل أو غير مبنيّ · ⚠ افتراض

---

## Locked — Do Not Reopen

قرارات حاكمة. لا تُناقَش ولا تُعكَس ولا يُعاد اعتمادها. عند تعارض أي ملفّ أو رسالة
تاريخية معها، **هذه القائمة و`PROJECT-STATE.md` يغلبان**.

1. **«شركة سعودية ريادية ومبتكرة» صياغة مسموحة.** «ريادية» = ريادة أعمال.
   المحظور: «الأولى» و«الرائدة» والتفوّق بلا سند. ⛔ لا استبدال شامل لـ«ريادية» بـ«متخصصة».
2. **«ازرع بذكاء.» الشعار الرئيسي.** «نَبني · نُوطّن · نُطوّر» منهج داخل الأقسام، لا شعار منافس.
3. **iGarden تبني منظومات متكاملة:** مشروع جديد من التخطيط والإنشاء، أو منشأة قائمة
   تُطوّر مرحلياً. الرقمنة **أولوية تجارية**، لا تعريف الشركة كلّه.
4. **الجمهور:** B2B أولاً · B2G ثانياً · B2C ثانوي عبر المتجر و`/home-solutions`.
5. **المصطلح المعتمد:** «مرفق R&D في عسفان». ⛔ لا «محطة عسفان» ولا Showroom.
6. **العضوية تطبيق ميداني؛ Bioponics قيد التطوير** — لا حلّ جاهز للتسليم، ولا اعتماد غير موثّق.
7. **الصور الحالية placeholders** حتى تمريرة الصور النهائية. ⛔ لا يُحذف موضع صورة قائم.
8. **وصف القدرة العامة لا يحتاج دليلاً بجواره.** الدليل إلزامي عند: رقم · اسم عميل ·
   مشروع محدّد · ادّعاء وفر أو نتيجة كمّية.
9. **#41 و#42 مغلقان ومتجاوزان** — لا إعادة فتح ولا إحياء لشقّ «ريادية→متخصصة».
10. **`sitemap` الصحيح = 25.**
11. **CTA الأساسي الظاهر لكل صفحة واحد** من: تقييم · زيارة تقنية · واتساب.
    `CTA_IDS` قاموس تتبّع تقني منفصل، ⛔ ولا تُضاف إليه قيمة عشوائية.
12. **Breadcrumbs قاعدة** لكل صفحات Hub & Spoke الجديدة — تنفيذ لاحق حيث لا توجد.

---

## الجمهور وترتيبه

| # | الشريحة | السطح الأساسي |
|---|---|---|
| ١ | **B2B** — مزارع تجارية ومنشآت إنتاج | `/products` · `/how-we-work` · `/contact` |
| ٢ | **B2G** — جهات حكومية وبرامج | `/compliance` · `/fact-sheet` · ◐ صفحة B2G لم تُبنَ |
| ٣ | **B2C** — أفراد | `/home-solutions` · `shop.igarden.sa` · الفوتر والشريط الخدمي |

---

## شجرة المسارات المنشورة ✓

```
/                          الرئيسية
├── /products              بوّابة الحلول ← Hub
│   ├── /products/smart-greenhouses
│   ├── /products/hydroponics
│   ├── /products/smart-controllers
│   ├── /products/iot
│   └── /products/smart-os
├── /how-we-work           المساران ← Hub
├── /osfan-station         مرفق R&D في عسفان
├── /learn                 المعرفة ← Hub
│   ├── /learn/why-hydroponic-fail-summer
│   ├── /learn/hydroponic-vs-traditional
│   ├── /learn/when-do-you-need-smart-controller
│   ├── /learn/operational-logs-farm-decisions
│   └── /learn/daily-measurements-smart-farm
├── /about                 عن iGarden
├── /compliance            السجلات وجاهزية الامتثال
├── /contact               نقطة التحويل
├── /fact-sheet            بيانات الشركة الرسمية
├── /home-solutions        للأفراد
└── /app                   تطبيق iGarden — «قريباً»
```

### رئيسية · ثانوية · قانونية

| الفئة | المسارات |
|---|---|
| **رئيسية** (في التنقّل) | `/products` · `/how-we-work` · `/osfan-station` · `/learn` · `/about` |
| **ثانوية** (في الفوتر والشريط الخدمي) | `/home-solutions` · `/app` · `/compliance` · `/fact-sheet` · `/contact` |
| **قانونية** (الشريط السفلي فقط) | `/privacy` · `/terms` · `/compliance-disclaimer` · `/data-charter` |
| **خارج التنقّل العام** | `/investors` · `/roadmap` — منشورتان لكن لا تُروَّجان في أي قائمة |

---

## هيكل الهيدر

```
┌─ Header — مستوى تنقّل واحد · sticky ───────────────────────────┐
│ [iGarden]   الحلول ▾ · كيف نعمل · مرفق عسفان · المعرفة ·        │
│             عن iGarden        [واتساب] [ابدأ مشروعك]           │
└────────────────────────────────────────────────────────────────┘
```

⛔ **شريط الخدمات (Utility Bar) حُذف** في Site Narrative Rebuild: كان مستوى
تنقّل ثانياً (45px على سطح المكتب) بلا وجهة فريدة. `TopBar.tsx` و`UTILITY_ITEMS`
حُذفا بعد إثبات صفر مستهلك — الاسترجاع من `git show 87cb99b:…`.

**وجهات الشريط بعد الحذف — كلّها محفوظة:**

| الوجهة | أين تعيش الآن |
|---|---|
| **Demo** | Mega Menu (عمود الرقمنة + الشريط السفلي) · MobileDrawer · footer · صفحات Smart OS وcompliance والمنتجات |
| **المتجر** | MobileDrawer · footer · `/home-solutions` · SpecializedSolutions |
| **للأفراد** | MobileDrawer · footer · **الشريط الختامي في الرئيسية** · SpecializedSolutions |
| **المعرفة** | Header الرئيسي |

- CTA الأساسي: `/contact?cta=readiness_assessment` · واتساب من `CONTACT.whatsapp`.
- **الرئيسية:** `-mt-[64px]` يمرّر الفيديو تحت الهيدر الشفّاف — سليم بعد الحذف.

## هيكل Mega Menu

يُفتح **بالنقر فقط** — ⛔ لا `onMouseEnter`. Escape يغلق · النقر خارجها يغلق ·
التركيز يعود إلى زرّ «الحلول». `<nav aria-label="الحلول">` — ⛔ لا `role="menu"` ناقص المنطق.
**أربعة أعمدة فعلية** (`grid-cols-4`).

**أعلى القائمة — المساران:** مشروع جديد → `/how-we-work#new-project` · منشأة قائمة → `/how-we-work#existing-facility`

| العمود | الروابط |
|---|---|
| ① أنظمة الإنتاج | المحميات الزراعية · أنظمة الزراعة المائية · الحلول المتخصصة → `/products#specialized` |
| ② التحكم والأتمتة | Smart Controllers · القياس وإنترنت الأشياء |
| ③ رقمنة تشغيل المزرعة | Smart OS · Demo مباشر · السجلات وجاهزية الامتثال |
| ④ الاستدامة وكفاءة التشغيل | الطاقة الشمسية المساندة · كفاءة المياه والطاقة |

**الشريط السفلي:** استعرض كل الحلول · كيف نعمل · Demo مباشر

> عمود ④ يستخدم **قيمة `cta` واحدة معتمدة** (`sustainability_solutions`) والتفريق في
> `topic=solar_support` / `topic=water_energy_efficiency` — يُحفظ ضمن `source_url`.
> ⛔ لا قيمتَي CTA جديدتين. ◐ مؤقّت حتى تُبنى صفحة الاستدامة.

---

## هيكل الجوال

الشريط الخدمي **لا يظهر كشرائح صغيرة أعلى الدرج**. الترتيب داخل `MobileDrawer`:

1. **الحلول** — Accordion: المساران، ثم العائلات الأربع بروابطها، ثم «استعرض كل الحلول»
2. كيف نعمل · مرفق عسفان · المعرفة · عن iGarden
3. **منصات iGarden** — Smart OS Demo · تطبيق iGarden «قريباً»
4. **للأفراد** — المتجر · Home Solutions
5. مثبّت أسفل الدرج: **ابدأ مشروعك** + **واتساب**

focus trap · Escape · إعادة التركيز إلى زرّ القائمة · كل هدف لمس ≥ 44px.

---

## هيكل الفوتر

شبكة 12 عموداً على سطح المكتب:

- **كتلة الهوية — 4/12:** الشعار · «ازرع بذكاء.» · وصف قصير · «ابدأ مشروعك» ·
  واتساب/الهاتف/البريد · أيقونات التواصل.
  العنوان التفصيلي يبقى في `/contact` — ⛔ لا كتلة مستقلة له.
- **التنقّل — 8/12 بثلاثة أعمدة:** الحلول · الشركة والميدان · للأفراد والمنصات.
- **الشريط السفلي فقط:** الروابط القانونية الأربعة + CR/MISA/SAIP/Tax + حقوق النشر.
  ⛔ «قانوني» ليس عموداً مساوياً للحلول.

على الجوال: أعمدة التنقّل الثلاثة Accordions، وكتلة الهوية مفتوحة دائماً.

---

## ترتيب الرئيسية — الحاكم

| # | القسم | الدور |
|---|---|---|
| ① | **Hero** | الوصف ثم النطاق · إسناد الفيديو ظاهر للمرفق |
| ①ب | **رابط «تعرّف على iGarden»** | سطر نصّي وحده — ⛔ **لا شريط إثبات مؤسسي أسفل الهيرو**؛ الإثبات التفصيلي يبقى في الصفحات الداخلية (`/about` · `/fact-sheet` · `/compliance` · `/osfan-station`) |
| ③ | **StartScale** | بوّابتان: أؤسّس مشروعاً جديداً · أطوّر منشأة قائمة — الحكومي رابط ثانوي · ⛔ لا سلّم أحجام ولا بطاقة أفراد |
| ④ | **SolutionTracks** | العائلات الأربع بعناوين منفعة + حدّ نطاق واحد في النهاية |
| ④ب | **SpecializedCapabilities** | قسم مضغوط يُعيد استعمال عناوين `/products#specialized` — ⛔ بلا وصف جديد ولا وعد جاهزية |
| ⑤ | **FieldEvidence** | تنفيذ ميداني بصياغة عامة + صور منسوبة إلى مرفق R&D في عسفان — بلا اسم عميل أو مشروع محدد أو تقرير حالة |
| ⑥ | **DeliveryPaths + SmartOSFlow** | مساران متوازيان: مشروع جديد من الاحتياج إلى التشغيل الأولي، ومنشأة قائمة من قراءة التشغيل إلى السجل وبيانات التشغيل |
| ⑦ | **InnovationTracks** | العضوية «مشروع ميداني جارٍ» · Bioponics «قيد التطوير» |
| ⑧ | **KnowledgeGrid** | «قبل أن تشتري — اقرأ» |
| ⑨ | **AssessmentSection** | «لا تحتاج قراراً كبيراً لتبدأ — تحتاج تقييماً» |
| ⑩ | **شريط الأفراد** | بعد النموذج حصراً · سطران · روابط نصّية · **B2C شريط ختامي فقط** |

## طبقات الرسالة — Locked

⛔ **تمنع الجولة الخامسة.** ثلاث طبقات لا تُخلَط:

| # | الطبقة | المضمون | الحالة |
|---|---|---|---|
| ① | **الهوية** | شركة سعودية تخطّط وتُنشئ وتطوّر منظومات زراعية متكاملة للمشاريع الجديدة والمنشآت القائمة | **ثابتة — ⛔ لا تتغيّر** |
| ② | **القدرة** | العائلات الأربع + القدرات المتخصّصة — ما نستطيع تسليمه | شبه ثابتة |
| ③ | **الأولوية** | رقمنة التشغيل — ما نبيعه هذا الموسم | **متغيّرة** |

🔑 **القاعدة الحاكمة:**
⛔ **لا تُقدَّم الطبقة ③ تعريفاً للشركة.**
⛔ **ولا يُقدَّم مشروع واحد قالباً لكل قدراتها.**

## حارس المصداقية — أربع درجات

يحكم صياغة النصوص. ⛔ **لا يُعرض كوسم في كل بطاقة** — يظهر حيث يلزم فقط.

| الدرجة | المعنى | الصياغة المسموحة |
|---|---|---|
| **قدرة قائمة** | تعمل اليوم ضمن التجهيز المركَّب | إخبار مباشر بلا تحفّظ |
| **بحسب النطاق أو مع شركاء** | تُفعَّل حسب نطاق المشروع أو عبر شراكة | «بحسب نطاق المشروع» · «ضمن التجهيز المتفق عليه» |
| **تُصمَّم حسب الطلب** | لا منتج رفّي — تُبنى عند وجود احتياج | «تُصمَّم وفق احتياج المشروع» |
| **R&D قيد التطوير** | مسار بحثي لم يُسلَّم | «قيد التطوير» صراحةً · ⛔ لا وعد جاهزية |

---

## قاعدة النبرة — إلزامية لكل قسم وصفحة

**المنفعة ← الآلية ← التحقق ← حدّ نطاق واحد في نهاية القسم.**

- H1 يحمل المنفعة، والكلمة البحثية تبقى في H1 أو eyebrow — ⛔ لا يضرّ عنوان
  المنفعة بالـSEO.
- حدّ النطاق **مرة واحدة** في نهاية القسم المشترك، ⛔ لا في كل بطاقة.
- **حالة كل منتج تبقى على بطاقته نفسها** (متاح · بحسب النطاق · قيد التطوير) —
  لا تُجمَع في تذييل عام.
- CTA أساسي واحد ظاهر لكل صفحة · روابط الاستكشاف ليست أزراراً منافسة.

### دور كل صفحة

| الصفحة | الدور |
|---|---|
| `/` | القصة الكاملة من المنفعة إلى التقييم |
| `/products` | Hub المحفظة — «لا تطارد أكثر من جهة حين يتوقّف شيء» |
| `/how-we-work` | مسارا التنفيذ حسب نقطة البداية |
| spokes ×5 | العمق التقني بعنوان منفعة وكلمة بحثية |
| `/compliance` | تشغيل قابل للمراجعة — بثلاث حالات مفصولة |
| `/about` | الهوية والقصة — ⛔ ليست صفحة مبيعات ثانية |
| `/osfan-station` | Evidence/R&D — «حيث نختبر قبل أن نركّب عندك» |
| `/learn` | «قبل أن تشتري — اقرأ» · مرتَّبة بقرار العميل |
| `/contact` | نقطة التحويل — «لا تحتاج قراراً كبيراً لتبدأ» |
| `/home-solutions` · `/app` | **مسار ثانوي** — ⛔ لا ظهور في Header |

---

## Hub & Spoke

```
homepage ──┬── /products      (Hub الحلول)
           ├── /how-we-work   (Hub التنفيذ)
           ├── /about
           ├── /osfan-station
           └── /learn         (Hub المعرفة)

/products ─┬── العائلات الأربع → صفحات المنتجات الخمس
           ├── #specialized   → الحلول المتخصصة
           └── #start         → المساران في /how-we-work

/learn ────┬── خمسة مقالات
           └── كل مقال يربط الحلّ المرتبط في /products/*
```

---

## المراسي — الحالة الفعلية

| المرساة | الحالة | الملفّ |
|---|---|---|
| `/how-we-work#new-project` | ✓ مُتحقَّقة على الإنتاج | `src/app/how-we-work/page.tsx` |
| `/how-we-work#existing-facility` | ✓ مُتحقَّقة على الإنتاج | `src/app/how-we-work/page.tsx` |
| `/products#specialized` | ✓ مُتحقَّقة على الإنتاج | `src/components/sections/SpecializedSolutions.tsx` |
| `/products#start` | ✓ موجودة | `src/app/products/page.tsx` |
| `/products#families` | ✓ موجودة | `src/components/sections/PortfolioFamilies.tsx` |
| `#production-systems` | ◐ **مرساة مؤجّلة** — تُفعَّل عند بناء القسم | — |
| `#automation` | ◐ **مرساة مؤجّلة** — تُفعَّل عند بناء القسم | — |
| `#digitization` | ◐ **مرساة مؤجّلة** — تُفعَّل عند بناء القسم | — |
| `#sustainability` | ◐ **مرساة مؤجّلة** — تُفعَّل عند بناء القسم | — |

**قاعدة:** لا رابط في القوائم إلى صفحة غير موجودة، ولا `#hash` بلا `id` فعلي في HTML.
الروابط التي كانت ستستهدف المراسي المؤجّلة تشير إلى الصفحة الأمّ `/products` بلا hash.
⛔ لا يُنشأ قسم ولا يُعدَّل محتوى صفحة لإرضاء رابط قائمة.

---

## Legacy — `/solutions` و`/services`

مُتحقَّق على الإنتاج (2026-08-23):

| المسار | الرمز | الوجهة | الحكم |
|---|---|---|---|
| `/solutions` | 308 دائم | `/products` | ✓ |
| `/solutions/smart-controllers` | 308 دائم | `/products/smart-controllers` | ✓ |
| `/solutions/smart-greenhouses` | 308 دائم | `/products/smart-greenhouses` | ✓ |
| `/solutions/hydroponics` | 308 دائم | `/products/hydroponics` | ✓ |
| `/solutions/<أي slug آخر>` | **307 مؤقّت** | `/products` | ◐ fallback مقصود |
| `/services` · `/services/hardware` · `/services/compliance` · `/services/support` | 308 دائم | `/products` · `/products/iot` · `/compliance` · `/how-we-work` | ✓ |

- **صفر 404** — لا تسرّب زوّار من روابط حراج ولينكدإن القديمة ✓.
- التحويلات الدائمة في `next.config.ts`؛ الـfallback في `src/app/solutions/[slug]/page.tsx`.

**السياسة ✓ مثبَّتة (2026-08-23):** المسارات التاريخية **المعروفة** فقط = **308 دائم** صريح في
`next.config.ts` · الـslug **المجهول** يبقى **307 مؤقّت** إلى `/products` · ⛔ **لا تحويل wildcard
كامل إلى دائم** · صفر 404. أي slug تاريخي حقيقي غير مغطّى ⟹ يُبلَّغ ولا يُنفَّذ قبل قرار علي.
`/solutions/[slug]` مسار legacy **يحتاج جرداً لاحقاً، ولا يُروَّج له الآن** — لا يظهر في أي قائمة ولا في الـsitemap.

---

## Wave 2 — Image Requirements

توثيق. الدفعة #1 صُحِّحت بالكامل على `feat/image-pass-al-ahsa-tower` (تمريرة التصحيح 2026-08-26)
بحالة **`approved-preview`** — ⛔ **ليست `production-approved`** ولم تُدمج.
السجلّ الكامل بالبصمات وحدود الادّعاء: [`docs/IMAGE-ASSET-MANIFEST.md`](IMAGE-ASSET-MANIFEST.md).
⛔ لم يُحذف أي موضع صورة قائم، ولم يُحذف أي `CONTENT_NEEDED` عدا موضع صورة البرج
الذي أُغلق فعلياً بأصل حقيقي.

| الصفحة | الأصل المطلوب | الحالة |
|---|---|---|
| **Smart OS** | لقطة Dashboard | **`temporary-safe`** ✓ — `/smart-os/dashboard-preview.png` مُدخَلة في Wave 2C. **بيانات محاكاة معلَنة** داخل اللقطة: شارتا `Demo Mode` و`Simulated Readings` + `Not a Certification Substitute` + شريط عربي يصرّح بذلك. ⛔ لا تُقصّ الشارات |
| **Hydroponics — iGarden Tower** | صورة برج | **`approved-preview`** ✓ — `igarden-tower-product.webp` (1440×1440، **خلفية بيضاء استوديو حقيقية**) داخل Product Card مربّعة، وتفصيل المنفذ inset صغير داخلها. أُغلق به `CONTENT_NEEDED`. ⛔ البرج غير مزروع: ليس دليل محصول |
| **Hydroponics — Hero** | صورة صدر الصفحة | **`approved-preview`** ✓ — `home-greenhouse-hydroponic-rows.webp` قصّ 4:3 و`object-cover`. ⛔ **منشأة منزلية**: لا تُقدَّم كمشروع تجاري ولا كمشروع عميل |
| **Smart Greenhouses** | صورة محمية داخلية | **`temporary`** — `/images/osfan-full/04_greenhouse_wide.webp` منسوبة صراحةً لعسفان في الـHero والمعرض، **باقية كما هي**. ⛔ لا تُعرض كدليل مشروع عميل أو محمية تجارية مسلَّمة |
| **Smart Greenhouses — بطاقة الأحساء** | صورة تنفيذ ميداني | **`approved-preview`** ✓ — **بطاقة واحدة** 4:3 (`al-ahsa-greenhouse-layout.webp`) مستقلة عن `GALLERY`. ⛔ لا معرض · لا «محمية ذكية مسلَّمة» ولا «تشغيل آلي» |
| **Smart Controllers** | صورة المنتج · مخطط الربط · لقطة Dashboard | `missing` — الموضعان الأولان بلا أصل؛ ثلاثة `CONTENT_NEEDED` قائمة. ⛔ **لا تُملأ بأي صورة عتاد ميداني أو Bench** — الهوية غير محسومة |
| **IoT** | صورة وحدة/حساسات مركَّبة + مخطط اتصال | `missing` — ⛔ الصور المتاحة مكوّنات طرف ثالث في بيئة Bench ولا تثبت تركيباً ميدانياً |
| **الأحساء (FieldProjectCard)** | صورة مشروع | `none-on-homepage` ✓ — **أُزيل معرض الأحساء من الرئيسية**؛ معرض عسفان وحده يبقى. صورة الأحساء الوحيدة في الموقع هي بطاقة `/products/smart-greenhouses`. ⛔ لا معرض ثانٍ أسفل معرض قائم |
| **المشروع العضوي (InnovationTracks)** | صورة مشروع | `missing` — ⛔ نفس القيد؛ القسم يبقى نصّياً |
| **Compliance** | نموذج سجل أو تقرير منزوع بيانات العملاء | `missing` — **بعد توفّره فعلياً**، ⛔ لا صورة قبل ذلك |

**تنظيف التكرارات** (‏`iGarden_Asfan_Images/` و`responsive/` الفائضة و`preview-*` — نحو 13MB):
**P2 مؤجَّل إلى Branch Hygiene** ✓ — ⛔ ليس P0، ولا يُحذف أي أصل في موجات المحتوى.

الصور المؤقتة الباقية في `/products` و`/how-we-work` تبقى موسومة `imageStatus: "temporary"`
وتُجرَد بـ`rg 'imageStatus: "temporary",'`. وصورة مسار **المشروع الجديد** في
`/how-we-work` انتقلت إلى `imageStatus: "verified-field-preview"` — أصل ميداني حقيقي
منسوب، ⚠ بانتظار اعتماد الدمج. صورة مسار **المنشأة القائمة** (‏Smart OS) بلا تغيير.

---

## Claim Gate — Mandatory for Every New Public Page

⛔ **لا تُقبل صفحة عامة جديدة قبل جدول يجيب عن الاثني عشر:**

1. ما الذي **يعمل اليوم**؟
2. ما **المتاح بحسب نطاق المشروع**؟
3. ما **قيد التطوير**؟
4. ما **الدليل** على كل رقم أو نتيجة أو مشروع محدد؟
5. هل ذُكر **عميل أو موقع أو اسم** يحتاج موافقة؟
6. هل الصورة **حقيقية · مؤقتة · محاكاة · مفقودة**؟
7. هل **metadata وOG وSchema** تكرّر الادّعاء نفسه؟
8. ما **CTA الأساسي** ومعرّفه المعتمد؟
9. ما **الصفحة الأم** والروابط الداخلية؟
10. هل توجد **صياغة مطلقة**: كل · دائماً · تلقائياً · مكتمل · معتمد · جاهز · يضمن · يضاعف؟
11. هل **الوعد قريب التنفيذ** وله خطة فعلية؟
12. هل النصّ **يعرّف قدرة** أم **يدّعي واقعة محددة**؟

> قاعدة الإسناد: وجود العبارة في الكود **ليس دليلاً عليها**. الدليل = سند على القرص
> (‏`LEXICON` · `BRAND-BIBLE` · سجلّ قياس · وثيقة مصدر) أو تُعمَّم بلا رقم.

### ترتيب الصفحات الجديدة — مثبَّت

| # | الصفحة | الشرط |
|---|---|---|
| ١ | **الاستدامة** | أول صفحة جديدة — تُغلق وجهة عمود ④ في الميجا بدل `/contact` |
| ٢ | **الأنظمة المتخصصة** | الثانية |
| ٣ | **المشاريع** | **عند وصول صور وأدلة قابلة للنشر** لا قبله |
| ٤ | **الواحة** | **One-pager بيعي** قبل أي صفحة عامة |
| ٥ | **B2G** | بعد نضج المراجع الحكومية والقطاعية |

---

## المسارات المستقبلية ◐

| المسار | الغرض | الحالة |
|---|---|---|
| Evidence Pack / Projects | أدلّة ميدانية ودراسات حالة | لم يبدأ |
| Smart Agricultural Oasis | صفحة الواحة | لم تبدأ |
| Sustainability page | وجهة دائمة لعمود ④ بدل `/contact` | لم تبدأ |
| B2G page | سطح الجهات الحكومية | لم تبدأ |

---

## أولوية المصادر

`PROJECT-STATE.md` (الجذر · المضيف القانوني) > `docs/SITE-ARCHITECTURE.md` > Notion > افتراض.
