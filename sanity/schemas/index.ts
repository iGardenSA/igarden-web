import { pageSchema } from "./page";
import {
  heroSectionSchema,
  statsSectionSchema,
  featureGridSchema,
  ctaSectionSchema,
  richTextSectionSchema,
} from "./sections";

export const schemaTypes = [
  pageSchema,
  heroSectionSchema,
  statsSectionSchema,
  featureGridSchema,
  ctaSectionSchema,
  richTextSectionSchema,
];
